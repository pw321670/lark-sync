# Obsidian 飞书同步插件 - 用户指南

将您的 Obsidian 笔记自动同步到飞书云文档，支持跨设备访问和协作。

## 📋 功能特点

- ✅ **一键同步**：点击左侧边栏云朵图标即可开始同步
- ✅ **跨设备同步**：通过 OneDrive 同步授权信息，多设备无需重复授权
- ✅ **增量上传**：只上传有变化的文件，节省时间和带宽
- ✅ **灵活筛选**：支持排除模式和白名单模式，精确控制同步内容
- ✅ **后台同步**：不阻塞界面，同步期间可以正常使用 Obsidian
- ✅ **实时进度**：显示同步进度、速度和剩余时间

## 🚀 快速开始

### 第一步：安装插件

#### 方法1：手动安装

1. 下载最新版本的插件文件
2. 在你的 Obsidian 保险库中创建插件目录：
   ```
   你的保险库/.obsidian/plugins/sync-obsidian-feishu/
   ```
3. 将以下文件复制到插件目录：
   - `main.js`
   - `manifest.json`
   - `styles.css`

#### 方法2：开发模式安装

如果你是开发者或想测试最新功能：

1. 克隆此仓库到本地
2. 运行 `npm install` 安装依赖
3. 运行 `npm run build` 编译插件
4. 使用开发辅助脚本创建保险库链接：
   ```bash
   npm run vault:link -- -VaultPath "你的保险库路径"
   ```

### 第二步：启用插件

1. 打开 Obsidian，进入 **设置 → 社区插件**
2. 关闭 **安全模式**
3. 在已安装插件列表中找到 **"Sync Obsidian to Feishu"**
4. 点击右侧开关启用插件

## ⚙️ 配置指南

### 飞书应用配置

在使用插件前，你需要在飞书开放平台创建应用并获取相关凭证。

#### 1. 创建飞书应用

1. 访问 [飞书开放平台](https://open.feishu.cn/)
2. 登录并进入 **管理后台**
3. 点击 **创建企业自建应用**
4. 填写应用信息：
   - **应用名称**：Obsidian Sync（或其他名称）
   - **应用描述**：Obsidian 笔记同步工具
5. 点击 **创建**

#### 2. 配置应用权限

1. 进入应用详情页，找到 **权限管理**
2. 搜索并添加以下权限：
   - **文件**：`drive:drive`（查看和上传文件）
   - **文件**：`drive:drive:readonly`（查看文件）
   - **文档**：`space:document:retrieve`（检索文档）
3. 申请权限并等待审核通过

#### 3. 获取应用凭证

1. 进入 **凭证与基础信息**
2. 复制以下信息：
   - **App ID**：格式如 `cli_xxxxxxxxx`
   - **App Secret**：点击显示后复制

#### 4. 创建同步文件夹

1. 在飞书文档中创建一个文件夹用于存放同步的笔记
2. 进入文件夹，查看浏览器地址栏
3. 地址格式类似：`https://feishu.cn/drive/folder/folder_xxxxxxxxx`
4. 复制 `folder_xxxxxxxxx` 部分，这就是 **根文件夹 Token**

### 插件设置配置

1. 在 Obsidian 中打开 **设置 → 社区插件 → Sync Obsidian to Feishu**

#### 飞书应用配置

- **App ID**：粘贴飞书应用的 App ID
- **App Secret**：粘贴飞书应用的 App Secret
- **根目录 Token**：粘贴飞书文件夹的 Token
- **重定向 URI**：保持默认 `http://127.0.0.1:3333/callback`

**💡 提示**：配置完成后，点击 **测试连接** 验证配置是否正确。

#### 同步策略配置

**文件匹配模式**：
- **排除模式**（推荐）：同步所有文件，排除指定的文件/文件夹
- **白名单模式**：只同步指定的文件/文件夹

**同步模式**：
- **手动同步**：只有手动点击时才同步
- **自动同步**：文件变化时自动同步（暂未实现）
- **定时同步**：按设定的时间间隔自动同步

**文件大小限制**：超过此大小的文件将被跳过（默认 20MB）

**文件匹配规则**：
- 排除模式示例：
  ```
  .trash
  .obsidian/
  *.tmp
  ```
- 白名单模式示例：
  ```
  Documents/
  Projects/
  *.md
  ```

#### 高级配置

- **并发上传**：同时上传的文件数量（1-10，默认 3）
- **重试次数**：上传失败时的重试次数（0-10，默认 3）
- **重试延迟**：重试之间的延迟时间（100-10000毫秒，默认 1000）
- **日志级别**：日志详细程度（error/warn/info/debug）

## 🔐 授权流程

配置完成后，需要进行飞书授权：

1. 打开 **命令面板**（`Ctrl/Cmd + P`）
2. 输入 `Feishu` 找到飞书相关命令
3. 选择 **"开始飞书授权"**
4. 浏览器会自动打开飞书授权页面
5. 登录飞书账号并授权应用访问文件
6. 授权成功后，插件会自动保存授权信息

**跨设备使用**：
- 授权信息存储在 Obsidian 插件数据中
- 如果你的 Obsidian 保险库通过 OneDrive 同步
- 在其他设备上打开同一个保险库时，无需重复授权

## 📤 使用方法

### 开始同步

有三种方式可以启动同步：

#### 方式1：点击同步按钮
- 点击 Obsidian 左侧边栏的云朵图标 ☁️
- 图标会显示当前状态：
  - ☁️ 空闲
  - 🔄 同步中
  - ✅ 成功
  - ❌ 失败
  - ⚠️ 警告

#### 方式2：使用快捷键
1. 打开 **设置 → 快捷键**
2. 搜索 "Feishu"
3. 为 "开始同步" 命令设置快捷键
4. 按下快捷键即可启动同步

#### 方式3：使用命令面板
1. 按 `Ctrl/Cmd + P` 打开命令面板
2. 输入 `Feishu`
3. 选择 **"开始同步"**

### 同步过程

同步过程中你会看到：

**实时进度显示**：
- 当前同步的文件名
- 进度百分比
- 上传速度
- 已上传/跳过/失败的文件数量

**同步完成通知**：
- 成功：显示上传文件数、跳过文件数、耗时
- 失败：显示错误信息和恢复建议
- 部分失败：列出成功和失败的文件

### 进度管理

同步过程中支持以下操作：

- **暂停同步**：临时暂停同步，可以恢复
- **恢复同步**：继续之前暂停的同步
- **取消同步**：完全取消当前同步操作

## 🔧 配置管理

### 导出配置

1. 进入插件设置页面
2. 点击 **导出配置** 按钮
3. 配置会以 JSON 格式下载到本地
4. 建议定期导出配置作为备份

### 导入配置

1. 进入插件设置页面
2. 点击 **导入配置** 按钮
3. 选择之前导出的配置文件
4. 确认导入操作

### 恢复默认配置

如果配置出现问题，可以恢复默认设置：

1. 进入插件设置页面
2. 点击 **恢复默认配置** 按钮
3. 确认恢复操作

## 🛠️ 故障排除

### 常见问题

#### 1. 授权失败
**症状**：点击"开始授权"后出现错误

**解决方法**：
- 检查飞书应用配置是否正确
- 确认飞书应用权限已审核通过
- 检查网络连接是否正常
- 尝试清除授权后重新授权

#### 2. 同步失败
**症状**：同步过程中出现错误

**解决方法**：
- 检查网络连接
- 验证飞书文件夹 Token 是否正确
- 确认文件大小是否超过限制
- 查看错误通知中的详细信息

#### 3. 文件被跳过
**症状**：某些文件没有同步

**解决方法**：
- 检查文件匹配模式设置（排除 vs 白名单）
- 确认文件大小限制设置
- 查看同步日志中的跳过原因

#### 4. 跨设备授权不生效
**症状**：在其他设备上仍需重新授权

**解决方法**：
- 确认 Obsidian 保险库通过 OneDrive 同步
- 检查插件数据是否正确同步
- 确认使用的是同一个 Obsidian 保险库

### 错误代码说明

- **配置错误**：缺少必需的配置项或格式不正确
- **授权错误**：授权过期或无效，需要重新授权
- **网络错误**：无法连接到飞书服务器，检查网络连接
- **权限错误**：飞书应用权限不足，检查权限配置
- **文件错误**：文件读取失败或文件格式不支持

## 📊 高级功能

### 同步模式详解

#### 排除模式（推荐）
**适用场景**：想要同步大部分文件，只排除特定文件

**配置示例**：
```
文件匹配模式：排除模式
排除规则：
.trash                  # 排除回收站
.obsidian/              # 排除 Obsidian 配置文件夹
*.tmp                   # 排除所有临时文件
.DS_Store               # 排除 macOS 系统文件
Thumbs.db               # 排除 Windows 缩略图文件
```

**结果**：同步所有文件，除了上述列表中的文件

#### 白名单模式
**适用场景**：只想同步特定文件或文件夹

**配置示例**：
```
文件匹配模式：白名单模式
白名单规则：
Documents/              # 只同步 Documents 文件夹
Projects/               # 只同步 Projects 文件夹
*.md                    # 只同步 Markdown 文件
*.pdf                   # 只同步 PDF 文件
```

**结果**：只同步上述列出的文件和文件夹，其他文件不同步

### 性能优化建议

#### 大文件处理
- 设置合理的文件大小限制（建议 20-50MB）
- 超大文件可以考虑使用其他方式（如飞书桌面端）

#### 并发控制
- 网络良好时可以增加并发上传数（5-10）
- 网络不稳定时降低并发数（1-3）

#### 重试策略
- 网络不稳定时增加重试次数（5-10次）
- 增加重试延迟避免频繁请求（2000-5000毫秒）

## 📝 最佳实践

### 推荐配置

**日常使用配置**：
- 文件匹配模式：排除模式
- 同步模式：手动同步
- 文件大小限制：20MB
- 并发上传：3
- 重试次数：3
- 重试延迟：1000毫秒

**高性能配置**：
- 文件匹配模式：白名单模式
- 同步模式：手动同步
- 文件大小限制：50MB
- 并发上传：8
- 重试次数：5
- 重试延迟：2000毫秒

**稳定网络配置**：
- 文件匹配模式：排除模式
- 同步模式：手动同步
- 文件大小限制：20MB
- 并发上传：5
- 重试次数：10
- 重试延迟：3000毫秒

### 使用建议

1. **首次使用**：先使用排除模式同步小规模测试
2. **定期备份**：定期导出配置和重要文件
3. **监控同步**：关注同步结果通知，及时发现异常
4. **网络环境**：在网络稳定时进行大规模同步
5. **权限管理**：定期检查飞书应用权限是否有效

## 🔐 安全说明

### 数据安全

- **授权信息**：存储在本地 Obsidian 插件数据中，随 OneDrive 同步
- **应用密钥**：妥善保管 App Secret，不要泄露给他人
- **访问控制**：飞书应用权限按需申请，避免过度授权
- **网络安全**：建议在安全的网络环境中使用

### 隐私保护

- 插件只访问你有权限的飞书文件夹
- 同步的文件内容和文件名都会上传到飞书
- 建议不要同步包含敏感信息的文件
- 可以使用排除模式排除敏感文件

## 📞 技术支持

### 获取帮助

如果遇到问题：

1. **查看文档**：先阅读本指南的故障排除部分
2. **检查日志**：在插件设置中查看详细日志信息
3. **搜索问题**：在 GitHub Issues 中搜索类似问题
4. **提问题**：在 GitHub 提交 Issue，详细描述问题

### 反馈建议

欢迎提出建议和反馈：

- 功能需求
- 界面改进
- 性能优化
- 文档完善
- Bug 报告

---

**享受你的 Obsidian 笔记同步体验！** 🎉

如有任何问题，欢迎随时联系我们。