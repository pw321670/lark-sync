# 飞书OAuth重定向URL错误修复指南

## 错误信息
- **错误码**: 20029
- **错误描述**: 重定向 URL 有误，请联系应用管理员
- **当前URL**: `http://127.0.0.1:3333/callback`

## 问题原因
飞书应用后台配置的重定向URL与插件使用的不匹配，或者应用类型配置错误。

---

## 🔍 关键发现

**为什么使用本地HTTP服务器？**
- Obsidian插件**不支持**自定义URL协议（如 `obsidian://callback`）
- 必须使用本地HTTP服务器来接收OAuth回调
- 这是桌面应用的标准OAuth实现方式

**技术参考**：
- [Obsidian论坛讨论：插件应该支持URL回调处理](https://forum.obsidian.md/t/plugin-api-should-allow-to-handle-the-callback-of-a-url-callback-scheme/7753)
- 该功能请求（2020年）尚未实现，所以本地HTTP服务器是唯一方案

---

## ✅ 解决方案

### 第一步：确认应用类型

1. **登录飞书开放平台**
   - 访问：https://open.feishu.cn/
   - 进入你的应用管理后台

2. **检查应用类型**
   - 确认应用类型是**"自建应用"** 或 **"企业自建应用"**
   - **不要**选择"网站应用"或"移动应用"
   - 桌面应用通常归类为"自建应用"

### 第二步：配置重定向URL

#### 选项A：使用 127.0.0.1（推荐）
```
http://127.0.0.1:3333/callback
```

#### 选项B：使用 localhost（备选）
```
http://localhost:3333/callback
```

**建议**：同时添加两个URL，以提高兼容性

### 第三步：查找重定向URL配置位置

重定向URL配置可能在以下位置之一（取决于飞书后台版本）：

1. **权限管理** → **权限配置** → **网页授权** → **重定向URL**
2. **安全设置** → **重定向URL**
3. **应用配置** → **回调地址**
4. **SDK与API** → **登录与授权** → **重定向URL**

### 第四步：添加重定向URL

**格式要求**：
- ✅ 必须以 `http://` 开头（本地不能用 `https://`）
- ✅ 端口必须是 `3333`
- ✅ 路径必须是 `/callback`
- ✅ **不要有尾部斜杠**
- ✅ **不要有查询参数**

**正确的格式示例**：
```
http://127.0.0.1:3333/callback
http://localhost:3333/callback
```

**常见的错误格式**：
```
❌ https://127.0.0.1:3333/callback  (本地不能用https)
❌ http://127.0.0.1:3333/callback/  (有尾部斜杠)
❌ http://127.0.0.1:3333            (缺少/callback路径)
❌ http://127.0.0.1:3333/callback?param=value (有查询参数)
```

### 第五步：保存并生效

1. 点击"保存"或"添加"按钮
2. 如果有"发布"或"上线"流程，需要完成发布
3. **重要**：某些配置可能需要几分钟才能生效，请稍后重试

---

## 🚨 如果问题仍然存在

### 可能的原因

1. **应用类型错误**
   - 如果应用被标记为"网站应用"，飞书可能要求使用 https:// 公网URL
   - 需要重新创建应用，选择正确的类型

2. **权限不足**
   - 确认你有应用的管理权限
   - 某些配置可能需要企业管理员审批

3. **飞书平台限制**
   - 飞书可能对某些应用类型限制了本地回调URL
   - 需要联系飞书技术支持确认

### 联系飞书技术支持

如果以上方法都无效，准备以下信息联系飞书技术支持：

1. **应用信息**：
   - 应用ID（App ID）
   - 应用类型
   - 应用名称

2. **错误信息**：
   - 错误码：20029
   - 重定向URL：`http://127.0.0.1:3333/callback`
   - 错误发生时间

3. **问题描述**：
   ```
   我正在开发一个Obsidian桌面插件，需要通过飞书OAuth授权。
   由于Obsidian插件不支持自定义URL协议，必须使用本地HTTP服务器
   接收回调（http://127.0.0.1:3333/callback）。但是在配置重定向URL后，
   仍然收到错误码20029。请确认桌面应用应该如何配置重定向URL。
   ```

---

## 📚 技术说明

**为什么不能用自定义URL协议？**

大多数桌面应用可以使用自定义URL协议（如 `myapp://callback`）来接收OAuth回调，但Obsidian插件目前不支持这个功能。相关讨论：
- [Plugin API should allow to handle the callback of a url-callback-scheme](https://forum.obsidian.md/t/plugin-api-should-allow-to-handle-the-callback-of-a-url-callback-scheme/7753)

因此，我们使用本地HTTP服务器（127.0.0.1:3333）来接收飞书的授权回调。这是一个安全且标准的做法，被广泛应用于桌面应用和本地工具。

**为什么本地回调是安全的？**

- 127.0.0.1 是本地回环地址，只能从本机访问
- 外部网络无法访问本地HTTP服务器
- 授权码只在本地传输，不会暴露给公网
- 这是OAuth 2.0标准中为原生应用推荐的方案之一

---

## 验证步骤

配置完成后，重新测试：

1. 返回Obsidian
2. 打开插件设置 → 点击"测试连接"
3. 确认授权流程能正常启动
4. 完成授权后，检查是否显示"授权成功"

## 验证步骤

配置完成后，重新测试：
1. 返回Obsidian
2. 点击"测试连接"按钮
3. 确认授权流程能正常启动

## 技术说明

**为什么使用本地回调URL？**
- OAuth 2.0 标准流程需要一个回调URL来接收授权结果
- 使用本地HTTP服务器（127.0.0.1:3333）来接收飞书的授权回调
- 这是一个安全且标准的做法，用于桌面应用和本地工具

**为什么不能使用公开URL？**
- Obsidian插件运行在用户本地电脑
- 没有公网可访问的服务器
- 使用本地回调URL是最合适的方案
